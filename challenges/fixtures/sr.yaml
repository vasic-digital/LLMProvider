# LLMProvider round-276 challenge fixture — Serbian (sr).
#
# Drives the Challenge runner at challenges/runner/main.go through a
# real circuit-breaker + health-monitor + retry flow. Anti-bluff:
# every PASS in the runner is backed by a runtime invariant — never
# a metadata-only check.
locale: sr
provider_id: "test-sr-provider"
prompt: "Dodaj OAuth2 autentifikaciju u API gateway servis"
expect_circuit_state_initial: "closed"
expect_failure_threshold: 3
expect_health_status_initial: "unknown"
expect_retry_initial_delay_ms: 100
